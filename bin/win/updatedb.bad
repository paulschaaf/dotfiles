#!/bin/bash

#SEARCHPATHS="/ $HOME"

DRIVES=$(/homes/pschaaf/bin/win/drives --space)

# todo: use find
TEMPDIRS="`find / $DRIVES /usr /var -maxdepth 1 -type d \\( -name 'tmp' -o -name 'temp' -o -name 'RECYCLER' -o -name 'Recycled' \\) -printf '%p ' 2>/dev/null`"

cd /
# echo $TEMPDIRS
nice --adjustment -19 /usr/bin/updatedb --localpaths="/" --prunepaths="$TEMPDIRS /e/MyDocuments"
