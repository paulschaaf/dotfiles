#!/bin/bash

target=~/bin/quick-ssh/config.${1%% *}

echo "## Custom config
Host $1
   HostKeyAlias $1
   HostName $2
   ForwardAgent yes
   ForwardX11 yes

" >| $target
cat ~/.ssh/config.default >> $target
