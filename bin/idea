#!/bin/bash

# set up the environment given the current directory
source ~/bin/gwSetEnv ${*}

if [ -z "$IDEA_HOME" ]; then
    echo
    echo IDEA_HOME must be specified!
else
    echo Starting ./idea.sh from $IDEA_HOME/bin
    cd $IDEA_HOME/bin
    exec ./idea.sh
fi
